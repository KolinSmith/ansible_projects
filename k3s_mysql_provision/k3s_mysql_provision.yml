---
  - hosts: provision_servers
    become: yes
    vars_files:
      - vars/main.yml
      - /home/dax/code_base/ansible_projects/vars.yml
    roles:
      - role: ansible-role-mysql

    tasks:
      # Allow all access to tcp port 80:
      - ufw:
          rule: allow
          port: "80"
          proto: tcp
